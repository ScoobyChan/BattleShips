from Game import Init       # imports Init to be used by Group class
from Group import Group     # imports Group class

start = Group() 			# Creates a variable called start that uses the class Group which has inheritated Init Too
start.start()				# This uses start to call the function start from Group	
